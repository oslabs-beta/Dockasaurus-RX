import type { Color, ThemeOptions } from '@mui/material';
import { PaletteOptions as MuiPaletteOptions } from '@mui/material/styles/createPalette';
type CustomColor = Pick<Color, 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800>;
interface DockerPalette {
    amber: CustomColor;
    blue: CustomColor;
    green: CustomColor;
    grey: CustomColor;
    red: CustomColor;
    violet: CustomColor;
}
declare module '@mui/material/styles/createPalette' {
    interface Palette extends MuiPaletteOptions {
        docker: DockerPalette;
    }
}
export interface ThemeOptionsBag {
    readonly dark: ThemeOptions;
    readonly light: ThemeOptions;
}
export {};
